@model IReadOnlyList<RazzaCaninaViewModel>

@*<div class="table-responsive">
    <table class="table table-striped table-hover">
    <thead>
    <tr>
    <th scope="col">Nome</th>
    <th scope="col">Razza per</th>
    <th scope="col">Gruppo</th>
    <th scope="col">Vita</th>
    <th scope="col">Origini</th>
    <th></th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
    <tr>
    <td>@item.name</td>
    <td>@item.bred_for</td>
    <td>@item.breed_group</td>
    <td>@item.life_span</td>
    <td>@item.origin</td>
    <td><a href="#" class="btn btn-sm btn-link float-end" onclick="openModaleDettaglioRazza('@item.name', '@item.temperament', '@item.img')" title="Dettaglio Razza"><i class="fa-solid fa-circle-info"></i></a></td>
    </tr>
    }
    </tbody>
    </table>
    </div>*@


@if (Model.Count == 0)
{
    <div class="container">
        <div class="section-title">
            <h2>@ViewData["Title"]</h2>
            <p>Nessuna razza canina trovata</p>
        </div>
    </div>

}
else
{
    <!-- ======= Team Section ======= -->
    <section id="team" class="team">
        <div class="container">
            <div class="row d-flex justify-content-center">
                @foreach (var item in Model)
                {
                    <div class="col-lg-4 col-md-6 d-flex align-items-stretch">
                        <div class="member">
                            <img src="@item.img" alt="">
                            <h4>@item.name</h4>
                            <span>Vita: @item.life_span</span>
                            <p>
                                @item.temperament
                            </p>
                            <div class="social">
                                <a><i class="fa-solid fa-paw"></i></a>
                                <a><i class="fa-solid fa-paw"></i></a>
                            </div>
                        </div>
                    </div>
                }
            </div>

        </div>
    </section>

    <!-- End Team Section -->
}